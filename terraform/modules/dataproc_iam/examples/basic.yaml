project_id: "project_id"
region: "europe-west2"

terraform:
  required_version: ">= 1.14.0, < 2.0.0"
  providers:
    google: ">= 7.17.0, < 8.0.0"

dataproc_iam:
  source: 
    version: v1.0.3
  spec:
    - cluster_name: "test_cluster"
      iam:
        "roles/dataproc.editor":
          - "user:USER_EMAIL"