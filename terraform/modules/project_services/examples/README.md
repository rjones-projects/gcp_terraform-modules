# project_services YAML Examples

These examples show how to invoke the project_services module using the YAML config
that is converted into `terraform.tfvars.json`

Usage:

- Update paths if needed.
- Run the generator from repo root:
  `python3 yaml_to_tfvars.py modules/project_services/examples/one_api.yaml deploy/terraform.tfvars.json`