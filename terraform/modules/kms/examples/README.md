# KMS YAML Examples

These examples show how to invoke the KMS module using the YAML config
that is converted into `terraform.tfvars.json`

Usage:

- Update paths if needed.
- Run the generator from repo root:
  `python3 yaml_to_tfvars.py modules/kms/examples/basic.yaml deploy/terraform.tfvars.json`
