# NGDI FinOps Labels module (spec-based wrapper).
# No resources are created here; this module only validates and normalizes labels
# according to the FinOps policy defined in locals.tf and exposes the result as outputs.

